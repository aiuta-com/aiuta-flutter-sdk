/// Consent type used in the SDK.
enum AiutaAnalyticsConsentType {
  /// Consent embedded into onboarding flow.
  embeddedIntoOnboarding,

  /// Consent as a standalone onboarding page.
  standaloneOnboardingPage,

  /// Consent as a standalone image picker page.
  standaloneImagePickerPage,
}
