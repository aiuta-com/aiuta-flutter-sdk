/// Enum representing the types of customization available in the Aiuta system.
enum AiutaCustomizationType {
  /// Represents a built-in implementation that is pre-configured and provided by the system.
  builtIn,

  /// Represents a custom implementation that can be defined and configured by the user.
  custom,
}
