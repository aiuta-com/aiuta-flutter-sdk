/// Enum representing the types of data providers available in the Aiuta system.
enum AiutaDataProviderType {
  /// Represents a built-in data provider that is pre-configured and provided by the system.
  builtIn,

  /// Represents a custom data provider that can be implemented and configured by the user.
  custom,
}
